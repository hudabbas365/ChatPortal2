<UserControl x:Class="GatewayApp.Views.DatasourcesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Datasources" FontSize="26" FontWeight="Bold" Margin="0,0,0,12" />
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Add Datasource" Command="{Binding OpenWizardCommand}" Style="{StaticResource PrimaryButtonStyle}" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="8,0,0,0" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding Connections}" AutoGenerateColumns="False" Style="{StaticResource DataGridStyle}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="180" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
